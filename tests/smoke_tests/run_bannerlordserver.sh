#!/usr/bin/env bash
# ENABLED (AUTH): Bannerlord's dedicated Linux branch currently requires
# authenticated Steam or SteamCMD access to app 1863440 branch `linux_test`,
# and start still needs a TaleWorlds custom server token. See
# docs/servers/bannerlordserver.md for the supported setup flow.
echo "Smoke test for bannerlordserver is ENABLED (AUTH) - authenticate Steam/SteamCMD for app 1863440 branch linux_test and provide a TaleWorlds custom server token before starting; see docs/servers/bannerlordserver.md"
exit 0
