# Disabled game server modules
#
# Each non-empty, non-comment line disables a game module.
# Format:  module_name	reason
# (tab-separated)
#
# When a user tries to create a server with a disabled module,
# the create command will refuse and print the reason.
#
# To re-enable a server, remove or comment out its line.

# Dead or gated download URLs
# ts3server fixed: scraper now matches linux_amd64 URL pattern directly
# ts3server	TeamSpeak downloads page layout changed; version scraper broken

# Platform or runtime failures
abfserver	SteamCMD app 2857200 returns Invalid platform on Linux
bobserver	SteamCMD app 882430 download timeout; likely too large for automated CI testing
# insserver fixed: CRLF stripped from srcds_run in valve_server.py install step
# insserver	Insurgency srcds_run has CRLF line endings; cannot exec
# inssserver fixed: exe_name now Insurgency/Binaries/Linux/InsurgencyServer-Linux-Shipping
# inssserver	Insurgency: Sandstorm (app 581330) module uses incorrect executable path; binary is at Insurgency/Binaries/Linux/ not install root
# HLDS mod servers: maps not bundled with the SteamCMD server download

# Servers that crash immediately in headless CI (Unity/Wine batch-mode window, Wine crashes)

# Servers that crash or stall during initialisation and cannot be made to work in CI

# Servers that require infrastructure or authentication unavailable in CI

# Servers with discontinued or unreachable downloads

# Servers that crash before creating their log files (UE4/Proton)

# Servers that time out loading assets in CI

# Servers that crash before creating their log files (Proton/Wine games below)

# Servers with port or query configuration issues
counterstrikeglobaloffensive	SteamCMD app 740 installs legacy CS:GO build 1575; server reaches Steam, receives MasterRequestRestart, and self-shuts down while hibernating. Use counterstrike2 for the current CS2 dedicated-server flow on app 730.
