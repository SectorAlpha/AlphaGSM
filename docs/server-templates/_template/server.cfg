// Copy this seed only when the game really uses a stable config file with this name.
// Rename the file to the exact runtime filename and keep only keys the game or module supports.

hostname "AlphaGSM <server name>"
port "27015"
sv_maxplayers "16"
map "example_map"
sv_password ""