# Copy this seed only when the module does not manage one stable runtime config filename.
# Replace the keys below with the AlphaGSM-managed values that the module actually exposes.

servername="AlphaGSM <server name>"
port=27015
queryport=27016
maxplayers=16
map="example_map"
password=""