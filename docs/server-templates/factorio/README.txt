AlphaGSM does not yet ship a live Factorio server module.

This directory reserves the future template slot so the docs catalog stays complete.
When Factorio server support lands, replace this note with the real managed config sample or with a module-specific reference note if the upstream server generates its own config files.