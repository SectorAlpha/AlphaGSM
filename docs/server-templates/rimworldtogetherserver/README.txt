This directory includes the real RimWorld Together JSON config files that AlphaGSM keeps in sync.

Use `ServerSettings.json` under `Config/` and `ServerConfig.json` under `Configs/` as the starting point for the port default AlphaGSM manages.
