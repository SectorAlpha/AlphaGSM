This directory includes Starbound's runtime server config example.

Use `storage/starbound_server.config` as the starting point for the dedicated server's JSON settings.
Starbound reads this file from the `storage/` directory at runtime.
