This directory includes Nightingale's runtime server config example.

Use `NWX/Config/ServerSettings.ini` for native game settings such as `Password` and `AdminPassword`.
AlphaGSM still manages the install path, save slot, and launch port through the module datastore and start command.
