This directory includes a config example for this server.

The included file mirrors the real Cfx server `server.cfg` used for RedM.
Use it as the starting point for the values AlphaGSM exposes through setup, set, and start.
