This directory includes ASKA's native `server properties.txt` layout.

AlphaGSM preserves upstream comments and unmanaged world settings while syncing
the server identity, password, ports, Steam login token, and matchmaking region.
