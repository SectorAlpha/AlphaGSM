Killing Floor uses KillingFloor.ini, but this server is currently disabled in the
repo because the dedicated server flow requires external access/auth constraints.

This template directory keeps the canonical filename visible:
- KillingFloor.ini

Use an upstream/generated KillingFloor.ini as the authoritative base if you are
enabling the server manually outside the current CI-supported lifecycle.