# Archive Notes

This directory keeps legacy repository materials that are no longer part of the
active root-level project surface but may still be useful for historical
context.

- `README-legacy.txt` is the older pre-`README.md` top-level introduction.
- `changelog-legacy.md` is the older historical changelog file that predates
  the current `changelog.txt` discipline.
