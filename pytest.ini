[pytest]
timeout = 10
markers =
    integration: slow end-to-end tests that use real external processes or network access
    backend_integration: end-to-end tests for process backends (screen, tmux, subprocess)
