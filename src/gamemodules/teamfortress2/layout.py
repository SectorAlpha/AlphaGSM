ALLOWED_TF2_DESTINATIONS = ("tf/addons", "tf/cfg", "tf/maps", "tf/custom")

# Mod archives (SourceMod, MetaMod, plugins) are packaged relative to the
# game directory (tf/), not the server root.  Allowed paths here are the
# first-level directories inside tf/ that curated mod archives may populate.
ALLOWED_TF2_MOD_DESTINATIONS = ("addons", "cfg")
